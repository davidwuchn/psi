# State

Current truth about the Psi system.

---

- ✓ Canonical-root cleanup docs/tests now match the hosted runtime architecture (2026-03-17, commit `d037818`): agent-session test support now includes canonical-root-backed fixture helpers (`make-session-ctx`, `set-state!`, `update-state!`), representative runtime/executor tests now use those fixtures instead of old atom-shaped setup, and subsystem comments now describe `:session-data-atom` / `:ui-state-atom` as adapter-backed compatibility views over canonical state rather than independent sources of truth. Focused lint/paren verification is green across the touched production and test files.

- ✓ Runtime-visible ui/recursion/nrepl/oauth projections now converge on canonical state (2026-03-17, commit `a110370`): extension UI state now lives under canonical root state and is exposed through a compatibility atom adapter, recursion state is hosted via `recursion/create-hosted-context` backed by the canonical session root, canonical runtime-visible nREPL metadata is written/read through the session state root, and runtime-visible OAuth metadata now tracks authenticated providers, pending login, and last-login fields in canonical state while secure OAuth store/provider mechanics remain outside the root. Focused lint/paren verification is green across `core.clj`, `main.clj`, `resolvers.clj`, `rpc.clj`, and `recursion/core.clj`.

- ✓ Agent-session mutable runtime state now converges on a canonical root-state model (2026-03-17, commit `3097239`): `spec/system-context-unification.allium` now defines the single-root state model and is integrated into `spec/session-core.allium`, `spec/coding-agent.allium`, and `spec/rpc-edn.allium`. `components/agent-session/src/psi/agent_session/core.clj` now owns a canonical `:state*` root plus path-based helper accessors, and session-owned mutable state has been migrated off many direct context atoms across `core.clj`, `runtime.clj`, `executor.clj`, `resolvers.clj`, and `persistence.clj`. Migrated state domains include session data, context index, journal/flush state, turn context, provider captures, tool-call attempts, tool-output telemetry, and background jobs. Runtime handles remain separate where appropriate (agent-core, extension/workflow registries, UI integration handles, OAuth runtime/store integration, nREPL runtime integration). Cross-file lint is clean and paren repair is clean across the touched agent-session file set.

- ✓ Provider captures now support exact turn-id lookup in code/tests (2026-03-17, commit `2413557`): `components/agent-session/src/psi/agent_session/resolvers.clj` now includes narrow lookup resolvers for one provider request or one provider reply keyed by turn id, exposed as `:psi.agent-session/provider-request-for-turn-id` and `:psi.agent-session/provider-reply-for-turn-id` from `:psi.agent-session/lookup-turn-id`. Focused regression coverage in `components/agent-session/test/psi/agent_session/core_test.clj` proves exact Anthropic-style request/reply retrieval without dumping the whole capture tail. Current live-session caveat: the code/test surface is ready, but the currently attached app-query runtime has not yet reflected these attrs after in-repo reload attempts, so a true live runtime restart/reload is still required before the new lookup surface appears in `:psi.graph/root-queryable-attrs`.
- ✓ Provider error replies now survive subsequent stream churn and remain graph-discoverable after reload/rebuild (2026-03-17, commit `231477a`): `components/agent-session/src/psi/agent_session/core.clj` and `executor.clj` now keep a dedicated `:provider-error-replies-atom`, so provider `:error` captures are retained separately from the high-volume general provider reply stream that OpenAI delta traffic can quickly evict. `components/agent-session/src/psi/agent_session/resolvers.clj` now exposes `:psi.agent-session/provider-last-error-reply` and `:psi.agent-session/provider-error-replies`, enriches assistant-derived API errors from matching provider reply captures by request id, and deduplicates assistant/provider views of the same failure. `components/introspection/src/psi/introspection/graph.clj` now flattens join-map resolver outputs during root-attr discovery, so nested attrs like the new provider error surfaces appear in `:psi.graph/root-queryable-attrs` after a live graph rebuild. Fresh post-reload Anthropic 400s are now queryable end-to-end with retained body/headers via `provider-last-error-reply`, `provider-error-replies`, and enriched `api-errors`.
- ✓ Anthropic error capture now preserves raw reply bodies and normalized request ids (2026-03-17, commit `0bc6fb5`): `components/ai/src/psi/ai/providers/anthropic.clj` now keeps provider error headers, raw body text, and parsed JSON body on emitted `:error` events instead of collapsing failures to a status/request-id summary alone. `components/agent-session/src/psi/agent_session/executor.clj` now keeps deeper provider capture tails (`100` requests / `1000` replies) so failing Anthropic turns survive longer in live diagnostics, and `components/agent-session/src/psi/agent_session/resolvers.clj` now parses request ids from both old raw header-map error text and the normalized `Error ... [request-id req_xxx]` suffix. Regression coverage in `components/ai/test/psi/ai/providers/anthropic_test.clj` and `components/agent-session/test/psi/agent_session/core_test.clj` is green via focused `clojure -M:test`.

- ✓ Agent-session runtime creation and bootstrap are now split by responsibility (2026-03-17, commit `2368583`): `components/agent-session/src/psi/agent_session/main.clj` now separates UI-specific session context creation from shared runtime bootstrap. `create-runtime-session-context` owns OAuth/session context creation, effective model/thinking-level resolution, first-session creation, and UI-specific inputs like `:ui-type` / `:event-queue`. `bootstrap-runtime-session!` now accepts an existing `ctx` and owns template/skill/extension discovery, prompt setup, tool/resolver bootstrap, memory sync, and startup rehydrate. Console/TUI/RPC entrypoints now choose their UI type explicitly before invoking the shared bootstrap. Focused verification is green via `clj-kondo --lint components/agent-session/src/psi/agent_session/main.clj` and `clj-paren-repair components/agent-session/src/psi/agent_session/main.clj`.
